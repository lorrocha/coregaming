<%= simple_form_for(@event) do |f| %>

  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :date %>
  <%= f.input :location %>
  <%= f.button :submit, class: 'button round' %>

<% end %>